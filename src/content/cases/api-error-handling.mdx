---
title: API 错误处理
description: 根据响应状态和数据字段判断请求是否成功，并把失败转成清楚的信息。
scenario: 调用天气、模型或后台接口时，返回值可能缺字段、失败或格式异常，业务代码需要能解释这些失败。
level: core
tracks:
  - web
  - automation
  - ai
concepts:
  - if-statement
  - try-except
  - dict
  - json
  - function
project: weather-notifier
projects:
  - weather-notifier
  - mini-web-api
libraries: []
people:
  - sebastian-ramirez
  - armin-ronacher
codeVersions:
  - label: standard
    code: |
      import json

      def parse_response(raw):
          try:
              data = json.loads(raw)
          except json.JSONDecodeError:
              return {"ok": False, "error": "invalid json"}

          if data.get("status") != "ok":
              return {"ok": False, "error": data.get("message", "unknown error")}

          return {"ok": True, "data": data["data"]}

      print(parse_response('{"status": "error", "message": "city missing"}'))
pitfalls:
  - 只判断 HTTP 200 会漏掉超时、空响应和 JSON 解析失败。
extensions:
  - 加入超时、重试、日志和结构化错误对象，让调用方能区分网络失败与业务失败。
---

API 错误处理让 `if` 和异常处理分工清楚：`try-except` 管格式和运行失败，`if` 管业务状态。
