---
title: 批量改名文件
description: 遍历目录里的文件，按统一规则重命名。
scenario: 下载目录里有一批截图和报告，需要把文件名前缀统一成日期加序号。
level: core
tracks:
  - automation
concepts:
  - variable
  - for-loop
  - function
  - pathlib
project: file-organizer
projects:
  - file-organizer
libraries: []
people:
  - guido-van-rossum
codeVersions:
  - label: standard
    code: |
      from pathlib import Path

      def preview_renames(folder):
          for index, file in enumerate(Path(folder).glob("*.txt"), start=1):
              new_name = file.with_name(f"report-{index:03d}{file.suffix}")
              print(file.name, "->", new_name.name)

      preview_renames(".")
pitfalls:
  - 直接改名且没有 dry-run 会把错误规则一次性应用到整批文件，轻则丢失命名线索，重则覆盖已有文件并让用户没有回滚路径。
extensions:
  - 增加 dry-run 预览、冲突检测和改名日志，必要时支持按日志撤销。
---

`pathlib` 的关键优势是让路径操作从字符串拼接变成对象方法。先预览，再真正 `rename`，也是自动化脚本的好习惯。
