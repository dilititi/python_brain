---
title: 继承
description: 让一个类复用或改写另一个类的行为。
summary: 继承让子类复用父类行为，并按需要覆盖或扩展。
whyImportant: 框架基类、异常类型、测试夹具和 GUI/游戏对象常用继承表达共同协议。理解它能帮助判断何时复用层级，何时改用组合。
definition: 继承是类之间的派生关系，子类可以获得父类的方法和属性，并按需覆盖。
mentalModel: 继承像在已有蓝图上做变体，保留共同结构，只改特殊部分。
category: oop
level: advanced
tracks:
  - beginner
  - web
  - ai
prerequisites:
  - class
related:
  - encapsulation
  - polymorphism
  - dunder-methods
  - dataclass
appliedIn:
  cases:
    - contact-records
  projects:
    - contact-book-cli
people:
  - guido-van-rossum
  - raymond-hettinger
worksRef:
  - id: the-python-tutorial-inheritance
    role: 官方教程展示单继承、多继承和方法查找。
history:
  - year: 2002
    event: 新式类成为现代对象模型基础：Python 2.2 引入新式类，逐步统一内置类型和用户自定义类的行为。
codeExamples:
  - title: 覆盖方法
    description: 子类可以复用父类结构，并改写某个方法。
    code: |
      class Animal:
          def speak(self):
              return "..."

      class Robot(Animal):
          def speak(self):
              return "beep"

      print(Robot().speak())
extends:
  - polymorphism
---
继承很强，但也容易让层级变复杂。新人阶段先理解“复用和覆盖”，等项目真的需要抽象层次时再深入多继承和 MRO。
