---
title: 字符串
description: 表示文本数据，支持索引、切片、查找、替换、分割和格式化。
summary: 字符串是不可变的 Unicode 文本序列，用来表示和处理文本。
whyImportant: 用户输入、文件内容、日志、路径片段和 API 字段大多先表现为字符串。理解不可变、切片和常用方法，是文本清洗的基础。
definition: 字符串是不可变的 Unicode 文本序列。
mentalModel: 字符串像一串固定顺序的字符。每次替换或拼接都会得到新字符串，而不是原地修改。
category: data-structure
level: intro
tracks:
  - beginner
  - web
  - data
  - automation
  - ai
prerequisites:
  - primitive-types
related:
  - f-string
  - regular-expression
  - bytes
appliedIn:
  cases:
    - text-normalizer
    - modern-syntax-upgrade
  projects:
    - python-basics-lab
    - cli-text-toolkit
people:
  - guido-van-rossum
works:
  - title: Text Sequence Type
    creator: Python Software Foundation
    type: book
    url: https://docs.python.org/3/library/stdtypes.html#text-sequence-type-str
    role: 官方文档说明 str 的方法和格式化能力。
history:
  - year: 2008
    event: Python 3 明确文本和字节分离：Python 3 用 str 表示 Unicode 文本，用 bytes 表示二进制数据。
codeExamples:
  - title: 清洗文本
    description: 常见字符串方法可以完成基础清洗。
    code: |
      raw = "  Python,Data,AI  "
      parts = raw.strip().lower().split(",")
      print(parts)
---
字符串是数据清洗、日志分析、接口参数和用户输入的核心材料。它和正则、f-string、bytes 都有强连接。
