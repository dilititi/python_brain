---
title: if 条件判断
description: 根据条件真假选择不同执行分支，是程序做决策的基础结构。
definition: if 语句会计算一个布尔条件，并只执行满足条件的代码块。
mentalModel: if 像路口的分岔牌，条件成立走一条路，不成立走另一条路。
category: control-flow
level: intro
tracks: [beginner, web, data, automation, ai]
prerequisites: [variable]
related: [while-loop, try-except]
expandsTo: [function]
appliedIn:
  cases: [input-validation, api-error-handling]
  projects: [contact-book-cli, mini-web-api]
people: [guido-van-rossum]
works:
  - title: "The Python Tutorial: if Statements"
    creator: Python Software Foundation
    type: book
    url: https://docs.python.org/3/tutorial/controlflow.html#if-statements
    note: 官方教程用 elif 和 else 展示多分支判断。
history:
  - year: 1991
    title: 基础控制流
    note: if 是 Python 早期就具备的基础控制结构。
codeExamples:
  - title: 分支
    description: 用 if/elif/else 把不同情况写清楚。
    code: |
      score = 82

      if score >= 90:
          level = "A"
      elif score >= 60:
          level = "pass"
      else:
          level = "retry"

      print(level)
---

`if` 的重点不是“判断大小”，而是把业务规则翻译成明确的分支。输入校验、权限判断、异常兜底和数据清洗都会反复用到它。
