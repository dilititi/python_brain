---
title: if 条件判断
summary: if 条件判断根据真假选择不同分支，让程序能做决策。
whyImportant: 输入校验、权限判断、错误处理和状态流转都需要分支。清楚条件表达式和缩进块，才能避免把业务规则写成混乱的嵌套。
definition: if 语句会计算一个布尔条件，并只执行满足条件的代码块。
mentalModel: if 像路口的分岔牌，条件成立走一条路，不成立走另一条路。
category: control-flow
level: intro
tracks:
  - beginner
  - web
  - data
  - automation
  - ai
prerequisites:
  - variable
related:
  - while-loop
  - try-except
appliedIn:
  cases:
    - input-validation
    - api-error-handling
  projects:
    - contact-book-cli
    - mini-web-api
people:
  - guido-van-rossum
worksRef:
  - id: the-python-tutorial-if-statements
    role: 官方教程用 elif 和 else 展示多分支判断。
history:
  - year: 1991
    event: 基础控制流：if 是 Python 早期就具备的基础控制结构。
codeExamples:
  - title: 分支
    description: 用 if/elif/else 把不同情况写清楚。
    code: |
      score = 82

      if score >= 90:
          level = "A"
      elif score >= 60:
          level = "pass"
      else:
          level = "retry"

      print(level)
extends:
  - function
---
`if` 的重点不是“判断大小”，而是把业务规则翻译成明确的分支。输入校验、权限判断、异常兜底和数据清洗都会反复用到它。
