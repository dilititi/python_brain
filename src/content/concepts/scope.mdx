---
title: 作用域
description: 决定一个名字在哪里可见，以及 Python 如何查找变量。
summary: 作用域决定名字在哪里可见，以及 Python 按什么顺序查找它。
whyImportant: 闭包、全局变量、函数参数和模块变量都会遇到名字查找。掌握 LEGB，才能解释变量为什么读得到、改不了或被意外遮蔽。
definition: 作用域是名字绑定的可见范围，Python 常用 LEGB 规则查找名字：Local、Enclosing、Global、Built-in。
mentalModel: 作用域像一层层抽屉，Python 会先找最近的抽屉，找不到再往外层找。
category: function
level: core
tracks:
  - beginner
  - web
  - data
  - automation
  - ai
prerequisites:
  - function
  - variable
related:
  - closure
  - function-parameters
appliedIn:
  cases:
    - function-call-playground
  projects:
    - python-basics-lab
    - cli-text-toolkit
people:
  - guido-van-rossum
  - raymond-hettinger
worksRef:
  - id: scopes-and-namespaces
    role: 官方教程解释局部、非局部和全局命名空间。
history:
  - year: 2001
    pep: PEP 227
    event: 嵌套作用域：PEP 227 让 Python 的嵌套函数作用域模型更完整。
codeExamples:
  - title: LEGB
    description: 内层函数可以读取外层函数里的名字。
    code: |
      def outer():
          label = "outer"

          def inner():
              return label

          return inner()

      print(outer())
---
作用域是闭包、装饰器和全局变量问题的前置节点。很多“变量为什么不是我想的值”都和作用域有关。
