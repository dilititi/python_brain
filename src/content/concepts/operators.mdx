---
title: 运算符
description: 用符号表达计算、比较、逻辑判断、成员关系和身份判断。
summary: 运算符用符号表达计算、比较、逻辑、成员和身份关系。
whyImportant: 条件判断、循环终止、集合过滤和数据计算都由运算符连接。分清 ==、is、in、and/or 等语义，能避开很多初学者隐蔽 bug。
definition: 运算符是作用在对象上的语法符号，例如 +、==、and、in、is。
mentalModel: 运算符像短动词，把值之间的关系和动作压缩成清晰符号。
category: syntax
level: intro
tracks:
  - beginner
  - data
  - automation
  - ai
prerequisites:
  - primitive-types
related:
  - if-statement
  - walrus-operator
  - type-casting
appliedIn:
  cases:
    - text-normalizer
    - collection-membership-report
  projects:
    - python-basics-lab
    - cli-text-toolkit
people:
  - guido-van-rossum
worksRef:
  - id: expressions
    role: Python 语言参考说明表达式和运算符优先级。
history:
  - year: 1991
    event: 表达式语法：运算符构成 Python 表达式系统的基础。
codeExamples:
  - title: 成员和逻辑
    description: in、and、or 常用于条件判断。
    code: |
      tags = {"python", "data"}
      is_ready = "python" in tags and len(tags) > 1
      print(is_ready)
---
学习运算符时不要只背符号，还要理解对象类型决定了运算含义。`+` 对数字是加法，对字符串是拼接。
