---
title: 函数
description: 把一段逻辑命名、参数化，并在多个地方复用。
definition: 函数是一段可调用对象，它接收输入，执行逻辑，并可返回结果。
mentalModel: 函数像一台小机器，把输入放进去，得到输出；机器本身也可以作为值传递。
category: function
level: intro
tracks:
  - beginner
  - web
  - data
  - automation
  - ai
prerequisites:
  - variable
  - for-loop
related:
  - lambda
  - closure
  - module
appliedIn:
  cases:
    - word-frequency
    - flask-routing
    - csv-cleaning
  projects:
    - text-analyzer
    - mini-web-api
people:
  - guido-van-rossum
works:
  - title: "The Python Tutorial: Defining Functions"
    creator: Python Software Foundation
    type: book
    url: https://docs.python.org/3/tutorial/controlflow.html#defining-functions
    role: 官方教程从斐波那契例子引出参数、返回值和默认值。
history:
  - year: 1991
    event: def 语法随语言发布：函数一直是 Python 把脚本组织成可维护程序的核心机制。
codeExamples:
  - title: 返回值
    description: 函数把过程包装成可以命名和测试的单元。
    code: |
      def total(numbers):
          result = 0
          for number in numbers:
              result += number
          return result

      print(total([3, 5, 8]))
extends:
  - decorator
  - class
---
函数是装饰器、闭包、测试夹具和 Web 路由的前置概念。理解“函数也是对象”之后，很多 Python 进阶语法会突然变得顺。
