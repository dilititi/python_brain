---
title: lambda
description: 创建一个短小的匿名函数，常用于排序键或回调。
definition: lambda 是 Python 的匿名函数表达式，只能包含一个表达式。
mentalModel: lambda 像临时写在现场的小函数，不值得单独起名时才用。
category: function
level: core
tracks: [data, ai]
prerequisites: [function]
related: [closure]
expandsTo: [decorator]
appliedIn:
  cases: [csv-cleaning]
  projects: []
people: [guido-van-rossum]
works:
  - title: "Python Tutorial: Lambda Expressions"
    creator: Python Software Foundation
    type: book
    url: https://docs.python.org/3/tutorial/controlflow.html#lambda-expressions
    note: 官方教程展示 lambda 如何作为小函数返回或传递。
history:
  - year: 1994
    title: 函数式工具进入语言
    note: lambda 和 map、filter、reduce 等函数式工具在早期 Python 中形成组合。
codeExamples:
  - title: 排序键
    description: lambda 常用于按某个字段排序。
    code: |
      rows = [{"name": "Ada", "score": 98}, {"name": "Guido", "score": 91}]
      print(sorted(rows, key=lambda row: row["score"]))
---

lambda 的价值是局部表达，不是炫技。逻辑一复杂，就应该回到 `def`。
